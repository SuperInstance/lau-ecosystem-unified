[package]
name = "lau-ecosystem-unified"
version = "0.1.0"
edition = "2021"
description = "THE SYNERGY CRATE — bridges all 320+ lau-* crates in the SuperInstance ecosystem"
license = "MIT"
repository = "https://github.com/SuperInstance/lau-ecosystem-unified"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
